const myHouse = new HouseBuilder(3)
    .addGarden()
    .addGarage()
    .build();

console.log(myHouse.describeHouse());

typescript
const myHouse = new HouseBuilder(3)
    .addGarden()
    .addGarage()
    .build();

console.log(myHouse.describeHouse());


House with 3 rooms, Garden: Yes, Garage: Yes

